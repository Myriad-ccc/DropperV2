<Window
    x:Class="DropperV2.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:DropperV2"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="DropperV2"
    Width="962"
    Height="898"
    Background="#141414"
    FontFamily="VCR OSD Mono"
    Foreground="White"
    KeyDown="Window_KeyDown"
    KeyUp="Window_KeyUp"
    ResizeMode="NoResize"
    UseLayoutRounding="True"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="64" />
            <RowDefinition Height="770" />
            <RowDefinition Height="64" />
        </Grid.RowDefinitions>

        <Grid
            x:Name="TitleBar"
            Grid.Row="0"
            Background="#323232"
            MouseDown="Form_MouseDown"
            MouseMove="Form_MouseMove"
            MouseUp="Form_MouseUp">

            <TextBlock
                x:Name="TitleTextShadow"
                Padding="5,5,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                FontSize="40"
                Text="DropperV2" />
            <TextBlock
                x:Name="TitleText"
                Padding="6,6,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                FontSize="40"
                MouseDown="TitleText_MouseDown"
                MouseMove="Form_MouseMove"
                MouseUp="Form_MouseUp"
                Text="DropperV2" />

            <StackPanel
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Orientation="Horizontal">

                <Button
                    x:Name="MinimizeButton"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Background="Transparent"
                    BorderBrush="{StaticResource MinimizeColor}"
                    BorderThickness="2"
                    Click="MinimizeButton_Click"
                    FontSize="48">
                    <Viewbox Width="48" Height="48">
                        <TextBlock
                            FontSize="48"
                            Foreground="{StaticResource MinimizeColor}"
                            Text="-"
                            TextAlignment="Center" />
                    </Viewbox>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button
                    x:Name="ClosingButton"
                    Margin="2,0,0,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Background="Transparent"
                    BorderBrush="{StaticResource ClosingColor}"
                    BorderThickness="2"
                    Click="ClosingButton_Click"
                    FontSize="48">
                    <Viewbox Width="48" Height="48">
                        <TextBlock
                            FontSize="48"
                            Foreground="{StaticResource ClosingColor}"
                            Text="X"
                            TextAlignment="Center" />
                    </Viewbox>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>

        <Border
            Grid.Row="1"
            BorderBrush="#282828"
            BorderThickness="1"
            RenderOptions.BitmapScalingMode="HighQuality"
            SnapsToDevicePixels="True">
            <UniformGrid x:Name="GameGrid" Loaded="GameGrid_Loaded" />
        </Border>

        <Grid
            Grid.Row="2"
            Background="#323232"
            MouseDown="Form_MouseDown"
            MouseMove="Form_MouseMove"
            MouseUp="Form_MouseUp" />

    </Grid>
</Window>
